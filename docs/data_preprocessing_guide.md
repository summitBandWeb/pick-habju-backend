# 합주실 데이터 전처리 가이드

> **목적**: Gemini 리서치 결과를 바탕으로 Pick 합주 시스템의 데이터 전처리 전략을 설명합니다.
> 왜 이렇게 설계했는지, 각 기술적 선택의 근거를 출처와 함께 제시합니다.
>
> **핵심 기술 스택**: Ollama + 8B 파라미터 로컬 LLM (Llama 3.1 8B, Gemma2 9B 등)

---

## 1. 왜 LLM 기반 전처리인가?

### 1.1 전통적 방식의 한계

네이버 합주실 데이터는 다음과 같은 특성을 가집니다:

| 데이터 특성 | 예시 | 전통적 파싱의 문제 |
|:-----------|:-----|:-----------------|
| **불규칙한 표현** | "최대 10명", "10인까지", "Max 10명" | 모든 패턴에 대한 정규식 작성 필요 |
| **중첩된 정보** | "[평일] 블랙룸 (4~6인 권장)" | 태그/인원 정보가 혼합되어 있음 |
| **암시적 맥락** | "당일 예약은 전화 문의" | 단순 키워드 매칭으로는 의미 파악 어려움 |

전통적 규칙 기반 크롤링은 **"취약성(Brittleness)"** 문제를 안고 있습니다. 레이아웃이 미세하게만 변경되어도 추출 로직이 붕괴될 수 있습니다.[^3]

### 1.2 LLM의 시맨틱 해석 능력

> "LLM 기반 추출은 '데이터가 어디에 있는가(Where)'가 아닌 **'데이터가 무엇을 의미하는가(What)'**에 집중합니다."
> — [Advanced LLM Web Scraping for DataOps](https://groupbwt.com/blog/llm-for-web-scraping/)[^5]

**실증적 효과:**
- 데이터 수집 시간 **약 25% 단축**
- 정확도 **10% 이상 향상**
- 이커머스 상품 정보 추출에서 **92% 정밀도** (기존 85% 대비)[^2]

### 1.3 왜 Ollama + 로컬 8B 모델인가?

| 비교 항목 | 클라우드 API (Gemini, GPT-4) | Ollama 로컬 LLM |
|:---------|:---------------------------|:----------------|
| **비용** | API 호출당 과금 | 초기 하드웨어 비용만 발생 |
| **Rate Limit** | 15 RPM (Gemini 무료) | 제한 없음 |
| **지연 시간** | 네트워크 의존 | 로컬 추론으로 일정함 |
| **데이터 프라이버시** | 외부 서버 전송 | 로컬에서만 처리 |
| **오프라인 사용** | 불가 | 가능 |

> "**7B 파라미터 수준의 소형 오픈소스 모델들도 4비트 양자화와 퓨샷 프롬프팅을 통해 상용 하드웨어에서 높은 정확도**로 개체 매칭을 수행할 수 있음이 입증되었다."
> — [Entity Matching with 7B LLMs (CEUR-WS)](https://ceur-ws.org/Vol-3931/paper4.pdf)[^22]

**8B 모델 추천:**

| 모델 | 특징 | 권장 사용 케이스 |
|:----|:-----|:---------------|
| **Llama 3.1 8B** | 범용성 높음, 한국어 지원 개선 | 기본 추천 |
| **Gemma2 9B** | Google 기반, 구조적 출력 우수 | JSON 추출 집중 |
| **Mistral 7B** | 빠른 추론 속도 | 대량 배치 처리 |

---

## 2. Pick 합주 시스템의 전처리 아키텍처

### 2.1 하이브리드 파이프라인 설계

```
┌────────────────────────────────────────────────────────────────────────────┐
│                      Pick 합주 데이터 전처리 파이프라인                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│   [네이버 GraphQL]  ──▶  [RoomParserService]  ──▶  [Data Preservation]    │
│                           ┌─────────────┐                                  │
│                           │ 1차: Ollama │                                  │
│                           │  8B LLM     │                                  │
│                           └──────┬──────┘                                  │
│                                  │실패 시                                   │
│                           ┌──────▼──────┐                                  │
│                           │ 2차: Regex  │                                  │
│                           │   Fallback  │                                  │
│                           └─────────────┘                                  │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 Ollama 연동 구현 예시

```python
import httpx
import json
from typing import Dict, Any

class RoomParserService:
    """Ollama 로컬 LLM을 사용하여 비정형 룸 정보를 구조화된 데이터로 변환합니다."""
    
    OLLAMA_URL = "http://localhost:11434/api/generate"
    MODEL_NAME = "llama3.1:8b"  # 또는 "gemma2:9b", "mistral:7b"
    
    async def parse_room_desc(self, name: str, desc: str) -> Dict[str, Any]:
        """룸 이름과 설명을 기반으로 구조화된 정보를 추출합니다."""
        
        prompt = self._build_prompt(name, desc)
        
        try:
            async with httpx.AsyncClient() as client:
                response = await client.post(
                    self.OLLAMA_URL,
                    json={
                        "model": self.MODEL_NAME,
                        "prompt": prompt,
                        "stream": False,
                        "format": "json",  # JSON 모드 강제
                        "options": {
                            "temperature": 0.1,  # 낮은 온도로 일관성 확보
                            "num_predict": 256   # 토큰 제한
                        }
                    },
                    timeout=30.0
                )
                
                result = response.json()
                return json.loads(result["response"])
                
        except Exception as e:
            logger.warning(f"Ollama parsing failed: {e}. Falling back to Regex.")
            return self._parse_with_regex(name, desc)
```

### 2.3 왜 이렇게 설계했는가?

#### Q: "LLM만 쓰면 되지 않나요?"

**A:** 소형 모델에는 운영상 주의점이 있습니다:
- **추론 품질 변동**: 8B 모델은 복잡한 문맥에서 실패 가능
- **하드웨어 의존**: GPU 메모리 부족 시 오류 발생
- **Ollama 서버 상태**: 서버 미실행 시 연결 실패

따라서 **LLM 우선 + Regex Fallback** 전략을 채택했습니다.

> **권장 사항**: "장애 대응을 위해 Regex Fallback을 추가하라"는 실무 패턴이 연구에서도 권장됩니다.[^3]

---

## 3. 소형 모델을 위한 프롬프트 엔지니어링

### 3.1 8B 모델의 특성 이해

대형 모델(70B+) 대비 8B 모델의 차이점:

| 특성 | 대형 모델 (70B+) | 소형 모델 (8B) |
|:----|:----------------|:--------------|
| **맥락 이해** | 긴 문맥도 잘 처리 | 짧고 명확한 지시 필요 |
| **추론 능력** | 복잡한 논리 가능 | 단순한 추출 작업에 적합 |
| **출력 일관성** | 높음 | 프롬프트 의존도 높음 |

> "소형 모델에서 BAML은 일반 JSON 스키마 방식보다 **약 5% 높은 신뢰도**를 보였다."
> — [DSPy BAML output format (Reddit/LocalLLaMA)](https://www.reddit.com/r/LocalLLaMA/comments/1moh7zx/dspy_baml_output_format_increases_reliability_of/)[^17]

### 3.2 8B 모델 최적화 프롬프트 설계 원칙

#### 원칙 1: 단순하고 명확한 지시

```python
# ❌ 나쁜 예: 복잡한 지시
prompt = """
다음 합주실 정보에서 데이터를 추출하여 JSON으로 반환해주세요.
값이 없거나 불확실하면 null로 표기하세요. 
만약 범위가 주어지면 중간값을 계산하고...
"""

# ✅ 좋은 예: 단순화된 지시
prompt = """
Extract data from this room info. Return JSON only.

Input:
- Room: {name}
- Desc: {desc}

Output format:
{"clean_name": "string", "max_capacity": number or null, ...}

Rules:
- Remove tags like [평일] from name
- "4~6인" → use middle value 5
"""
```

#### 원칙 2: Few-shot 예시 제공

8B 모델은 예시를 통한 학습이 효과적입니다:

```python
prompt = f"""
Extract room info as JSON.

Example 1:
Input: "[평일] 블랙룸", "최대 8명, 4~6인 권장"
Output: {{"clean_name": "블랙룸", "day_type": "weekday", "max_capacity": 8, "recommend_capacity": 5}}

Example 2:
Input: "화이트룸", "인당 3000원 추가"
Output: {{"clean_name": "화이트룸", "day_type": null, "extra_charge": 3000}}

Now extract:
Input: "{name}", "{desc}"
Output:
"""
```

> "퓨샷 프롬프팅을 통해 소형 모델의 정확도를 크게 향상시킬 수 있다."
> — [Entity Matching with 7B LLMs](https://ceur-ws.org/Vol-3931/paper4.pdf)[^22]

#### 원칙 3: JSON 모드 활용

Ollama는 `format: "json"` 옵션으로 JSON 출력을 강제할 수 있습니다:

```python
response = await client.post(
    OLLAMA_URL,
    json={
        "model": "llama3.1:8b",
        "prompt": prompt,
        "format": "json",  # JSON 형식 강제
        "options": {
            "temperature": 0.1  # 낮은 온도 = 일관된 출력
        }
    }
)
```

> "구조화된 출력(Structured Outputs)은 제약 디코딩 기술을 사용하여 **스키마 준수율을 100%에 가깝게** 끌어올립니다."
> — [Structured data extraction using LLM schemas](https://simonwillison.net/2025/Feb/28/llm-schemas/)[^12]

### 3.3 적용된 프롬프트 패턴

리서치에서 제시된 **4가지 프롬프트 패턴**[^11] 중 8B 모델에 적합한 패턴:

| 패턴 | 설명 | 8B 모델 적합도 | Pick 합주 적용 |
|:-----|:-----|:--------------|:--------------|
| **Semantic Extractor** | 의미 기반 추출 | ⭐⭐⭐ 적합 | ✅ 룸 정보 추출 |
| **Pattern Matcher** | 정규식 유사 템플릿 | ⭐⭐⭐ 매우 적합 | ✅ Regex Fallback |
| **Keyword Trigger** | 키워드 기반 범위 한정 | ⭐⭐⭐ 적합 | ✅ "당일", "전화" 탐지 |
| **Adaptive Extractor** | 동적 속성 발견 | ⭐ 부적합 | ❌ 8B에는 과도함 |

---

## 4. 데이터 품질 보장 메커니즘

### 4.1 Data Preservation 로직

#### Q: "LLM이 틀린 값을 반환하면 어떻게 되나요?"

**A:** **Data Preservation** 로직으로 수동 입력 데이터를 보호합니다.

```
   새로운 파싱 값         기존 DB 값           최종 저장 값
   ─────────────         ──────────           ────────────

   max_capacity = 1  +   max_capacity = 10  →  max_capacity = 10
   (파싱 실패/기본값)     (수동 입력)            (기존 값 유지)
```

**핵심 규칙**: 새 값이 기본값(0 또는 1)이고, 기존 값이 유효(>1)하면 → **기존 값 유지**

이는 리서치에서 강조한 **"정보의 손실을 방지하기 위해 항상 원본 데이터를 보존하는 전략"**과 일치합니다.[^7]

### 4.2 8B 모델 특화 검증 전략

소형 모델은 대형 모델보다 오류율이 높으므로 추가 검증이 필요합니다:

```python
def validate_parsed_result(result: Dict) -> bool:
    """파싱 결과의 유효성을 검증합니다."""
    
    # 1. 필수 필드 존재 확인
    required_fields = ["clean_name", "max_capacity"]
    if not all(field in result for field in required_fields):
        return False
    
    # 2. 값 범위 검증 (합주실 현실적 범위)
    max_cap = result.get("max_capacity")
    if max_cap is not None and (max_cap < 1 or max_cap > 50):
        return False  # 비현실적인 값
    
    # 3. 추가 요금 검증
    extra = result.get("extra_charge")
    if extra is not None and (extra < 0 or extra > 50000):
        return False  # 비현실적인 추가 요금
    
    return True
```

### 4.3 품질 평가 지표

리서치에서 제시된 평가 지표[^30]와 우리 시스템의 대응:

| 지표 | 정의 | Pick 합주 시스템 적용 |
|:----|:-----|:--------------------|
| **Field Accuracy** | 개별 필드 정확도 | Discord 알림으로 수동 검증 |
| **Faithfulness** | 원본과의 일치성 | Data Preservation으로 보장 |
| **Contextual Recall** | 필요 정보 추출 완전성 | Regex Fallback으로 보완 |

---

## 5. 정규화 기법 적용

### 5.1 수치형 데이터 정규화

합주실 데이터에서 정규화가 필요한 필드:

| 필드 | 입력 예시 | 정규화 결과 |
|:----|:---------|:-----------|
| `max_capacity` | "최대 10명", "10인까지" | `10` (정수) |
| `extra_charge` | "3,000원", "3000원" | `3000` (정수) |
| `recommend_capacity` | "4~6인" | `5` (중간값) |

```python
# 범위 표현 → 중간값 변환 (room_parser_service.py)
range_match = re.search(r'(\d+)\s*[~\-]\s*(\d+)\s*(?:인|명)?', desc)
if range_match:
    min_r = int(range_match.group(1))
    max_r = int(range_match.group(2))
    rec_cap = (min_r + max_r) // 2  # 중간값 계산
```

> "서로 다른 척도를 가진 특성들을 동일한 범위로 조정하는 것이 데이터 정규화의 일차적 목표입니다."
> — [Data Normalization Demystified](https://www.flagright.com/post/data-normalization-demystified-a-guide-to-cleaner-data)[^7]

### 5.2 텍스트 정규화

룸 이름에서 태그 제거:

```python
# "[평일] 블랙룸" → "블랙룸"
clean_name = re.sub(r'\[평일\]|\(평일\)', '', name).strip()
```

---

## 6. Ollama 운영 가이드

### 6.1 초기 설정

```bash
# 1. Ollama 설치 (Windows)
# https://ollama.com/download 에서 설치

# 2. 모델 다운로드
ollama pull llama3.1:8b      # 약 4.7GB
# 또는
ollama pull gemma2:9b        # 약 5.5GB

# 3. 서버 실행 확인
ollama serve  # 기본 포트: 11434
```

### 6.2 성능 최적화 옵션

```python
options = {
    "temperature": 0.1,     # 낮을수록 일관된 출력 (0.0 ~ 1.0)
    "num_predict": 256,     # 최대 생성 토큰 (적을수록 빠름)
    "num_ctx": 2048,        # 컨텍스트 길이 (8B 기본값)
    "num_gpu": 1,           # GPU 레이어 수 (0이면 CPU만 사용)
    "top_p": 0.9,           # 토큰 샘플링 범위
}
```

### 6.3 4비트 양자화 모델 사용

메모리가 제한된 환경에서:

```bash
# Q4 양자화 버전 사용 (메모리 약 50% 절약)
ollama pull llama3.1:8b-q4_0
```

> "4비트 양자화를 통해 상용 하드웨어에서도 높은 정확도로 실행 가능합니다."
> — [Entity Matching with 7B LLMs](https://ceur-ws.org/Vol-3931/paper4.pdf)[^22]

### 6.4 하드웨어 요구사항

| 구성 | 최소 사양 | 권장 사양 |
|:----|:---------|:---------|
| **RAM** | 8GB | 16GB |
| **VRAM** | 6GB (RTX 3060) | 8GB+ (RTX 3070+) |
| **저장공간** | 10GB | 20GB (여러 모델) |
| **CPU only** | 가능 (느림) | GPU 권장 |

---

## 7. 향후 개선 방향

리서치에서 제안된 고도화 기술 중 8B 모델에 적용 가능한 항목:

### 7.1 단기 개선 (권장)

| 기술 | 기대 효과 | 8B 모델 적합도 |
|:----|:---------|:-------------|
| **배치 파싱** | 처리량 ↑ | ⭐⭐⭐ 매우 적합 |
| **Few-shot 강화** | 정확도 ↑ | ⭐⭐⭐ 필수 적용 |
| **결과 캐싱** | 재처리 방지 | ⭐⭐⭐ 매우 적합 |

### 7.2 중장기 개선 (검토 필요)

| 기술 | 설명 | 적용 시점 |
|:----|:-----|:---------|
| **BAML 도입** | 소형 모델 JSON 추출 최적화 | 파싱 오류 빈번 시 |
| **모델 파인튜닝** | 합주실 도메인 특화 학습 | 데이터 축적 후 |
| **개체 해상도** | 중복 합주실 통합 | 데이터량 증가 시 |

> "대형 모델을 사용할 때 DSPy와 BAML 어댑터를 결합하면, 단순 수동 프롬프트 대비 **약 20% 이상의 정확도 향상**을 달성할 수 있다."
> — [DSPy Optimization (Kevin Madura)](https://kmad.ai/DSPy-Optimization)[^18]

---

## 8. 다단계 파싱 파이프라인 (Multi-Level Parsing)

> **목표**: LLM 호출을 최소화하면서 파싱 정확도 99%를 달성한다.

### 8.1 파이프라인 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                    다단계 파싱 파이프라인                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [입력] ──▶ [Level 1: Keyword Map] ──(성공)──▶ [결과 반환]      │
│                     │                                           │
│                   (실패)                                         │
│                     ▼                                           │
│             [Level 2: Regex] ──(성공)──▶ [결과 반환]            │
│                     │                                           │
│                   (실패)                                         │
│                     ▼                                           │
│             [Level 3: Noise 제거 + LLM] ──▶ [결과 반환]         │
│                     │                                           │
│                   (실패)                                         │
│                     ▼                                           │
│             [Level 4: Price Logic 검증]                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 Level 1: Keyword Map (규칙 기반)

**명확한 의미가 있는 키워드**만 매핑하여 즉시 추론합니다.

```python
KEYWORD_CAPACITY_MAP = {
    "대형": 15,
    "중형": 8,
    "소형": 4,
    "대합주실": 15,
    "소합주실": 4,
}
```

#### ⚠️ 의도적으로 제외한 키워드

| 제외 키워드 | 제외 이유 |
|:-----------|:---------|
| `L룸`, `S룸`, `M룸` | 합주실마다 의미가 다름 (S = Small 또는 Special) |
| `A룸`, `B룸` | 단순 넘버링, 크기와 무관 |
| `R룸` | Regular로 해석될 수 있으나 불명확 |

> **원칙**: 알파벳 한 글자로 표기된 룸 이름은 과감하게 제외한다. 노이즈가 섞이면 디버깅이 어려워진다.

### 8.3 Level 2: Regex Fallback

Keyword Map으로 해결되지 않으면 정규표현식 패턴 매칭을 시도합니다.

```python
# 지원하는 패턴
"최대 10인"     → max_capacity = 10
"10명까지"     → max_capacity = 10
"4~6인 권장"   → recommend_capacity = 5 (중간값)
```

### 8.4 Level 3: Noise Reduction + LLM

규칙 기반으로 해결되지 않는 "애매한 텍스트"는 LLM에게 맡기되, 노이즈를 제거하여 정확도를 높입니다.

```python
def _clean_text_for_llm(text: str) -> str:
    """LLM 입력 전 노이즈 제거"""
    import re
    
    # 1. HTML 태그 제거
    text = re.sub(r'<[^>]+>', '', text)
    
    # 2. 이모지 및 특수문자 제거 (한글, 영문, 숫자만 유지)
    text = re.sub(r'[^\w\s가-힣]', ' ', text)
    
    # 3. 연속 공백 정리
    text = re.sub(r'\s+', ' ', text)
    
    return text.strip()
```

**효과**: `"✨최대 10명🎉"` → `"최대 10명"`

> **Why?** 8B 모델은 컨텍스트 윈도우가 작아 불필요한 정보(노이즈)에 쉽게 혼란을 겪습니다. 입력 데이터를 깨끗하게 만들어야 핵심 정보(숫자)에 집중할 수 있습니다.

### 8.5 Level 4: Price Logic 검증 (Safety Net)

가격 정보를 안전장치로 활용하여 잘못된 파싱 결과를 걸러냅니다.

```python
def _flag_suspicious_result(max_capacity: int, price_per_hour: int) -> bool:
    """비현실적인 파싱 결과를 수동 검토 대상으로 플래그"""
    
    # 1인실인데 가격이 15,000원 이상? → 재검토 필요
    if max_capacity == 1 and price_per_hour > 15000:
        return True
    
    # 20인실인데 가격이 10,000원 미만? → 재검토 필요
    if max_capacity >= 20 and price_per_hour < 10000:
        return True
    
    return False
```

---

## 9. 트러블슈팅

### 8.1 일반적인 문제

| 증상 | 원인 | 해결 방법 |
|:----|:-----|:---------|
| `Connection refused` | Ollama 서버 미실행 | `ollama serve` 실행 |
| JSON 파싱 오류 | 모델이 비정형 출력 | `format: "json"` 옵션 추가 |
| 느린 응답 (30초+) | GPU 미사용 | `num_gpu` 옵션 확인 |
| 메모리 부족 | 모델 크기 초과 | Q4 양자화 버전 사용 |
| 한글 깨짐 | 인코딩 문제 | UTF-8 명시적 처리 |

### 8.2 8B 모델 한계 인지

```python
# 복잡한 추론이 필요한 경우 Regex Fallback 우선 고려
def should_use_llm(desc: str) -> bool:
    """LLM 사용이 적합한지 판단합니다."""
    
    # 단순한 패턴은 Regex로 충분
    simple_patterns = [
        r'최대\s*\d+',    # "최대 10명"
        r'\d+[~\-]\d+인',  # "4~6인"
    ]
    
    for pattern in simple_patterns:
        if re.search(pattern, desc):
            return False  # Regex로 처리 가능
    
    # 복잡한 문맥은 LLM 필요
    return True
```

---

## 9. 참고 문헌

[^2]: [Leveraging Large Language Models for Web Scraping (ResearchGate)](https://www.researchgate.net/publication/381372928_Leveraging_Large_Language_Models_for_Web_Scraping) - LLM 기반 크롤링의 시간 단축 및 정확도 향상 데이터

[^3]: [Effectiveness of traditional and LLM-based methods for web scraping (DEV Community)](https://dev.to/astro-official/effectiveness-of-traditional-and-llm-based-methods-for-web-scraping-dh6) - 전통적 방식의 취약성과 LLM의 내구성 비교

[^5]: [Advanced LLM Web Scraping for DataOps (GroupBWT)](https://groupbwt.com/blog/llm-for-web-scraping/) - 시맨틱 추출의 원리와 이점

[^7]: [Data Normalization Demystified (Flagright)](https://www.flagright.com/post/data-normalization-demystified-a-guide-to-cleaner-data) - 데이터 정규화 기법 및 원본 보존 전략

[^11]: [Prompt Patterns for Structured Data Extraction (WM.edu)](https://www.cs.wm.edu/~dcschmidt/PDF/Prompt_Patterns_for_Structured_Data_Extraction_from_Unstructured_Text___Final.pdf) - 구조적 추출을 위한 프롬프트 패턴 카탈로그

[^12]: [Structured data extraction using LLM schemas (Simon Willison)](https://simonwillison.net/2025/Feb/28/llm-schemas/) - 구조화된 출력과 스키마 준수

[^17]: [DSPy BAML output format (Reddit/LocalLLaMA)](https://www.reddit.com/r/LocalLLaMA/comments/1moh7zx/dspy_baml_output_format_increases_reliability_of/) - 소형 모델에서 BAML의 5% 신뢰도 향상

[^18]: [DSPy Optimization (Kevin Madura)](https://kmad.ai/DSPy-Optimization) - DSPy + BAML 결합 시 20% 정확도 향상

[^22]: [Entity Matching with 7B LLMs (CEUR-WS)](https://ceur-ws.org/Vol-3931/paper4.pdf) - 7B 모델의 4비트 양자화 및 퓨샷 프롬프팅 효과

[^30]: [LLM Evaluation Metrics (Confident AI)](https://www.confident-ai.com/blog/llm-evaluation-metrics-everything-you-need-for-llm-evaluation) - LLM 출력 품질 평가 지표

---

## 변경 이력

| 날짜 | 변경 내용 | 담당 |
|------|----------|------|
| 2026-02-05 | 초안 작성 - Ollama 8B 모델 기반으로 전면 수정 | - |

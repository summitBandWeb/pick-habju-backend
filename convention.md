# Pick-Habju Project Convention

이 문서는 프로젝트의 코드 품질, 커밋 일관성, 협업 워크플로우를 정의합니다. AI 에이전트 및 팀원은 반드시 아래 규칙을 준수해야 합니다.

## 0. 도입 배경 (Background)

최근 개발 환경에서 Cursor, Copilot 등 다양한 AI 도구의 활용이 보편화됨에 따라, 각자가 생성하는 코드와 주석 스타일이 파편화되어 전체적인 일관성이 저해되는 문제가 발생했습니다.

또한, 프로젝트 멤버의 변동 가능성과 신규 입사자의 온보딩 과정을 고려했을 때, 코드가 단순히 "어떻게(How)" 동작하는지를 넘어 **"어떤 목적으로(Why)"** 만들어졌고, **"어떻게 활용해야 하는지"**를 명확히 전달할 필요성이 커졌습니다.

이에 따라 우리는 **"코드 자체가 최고의 문서가 되도록"** 하면서도, AI와 사람이 모두 이해하기 쉬운 명확한 컨벤션을 수립하여 유지보수성을 높이고 협업 효율을 극대화하고자 합니다.

---

## 1. 코드 주석 (Code Comments)

**핵심 원칙: "무엇을(What)"보다 "왜(Why)"에 집중한다.**

### 1.1 Docstring (Google Style)

모든 공용 함수(Public Function)와 클래스에 필수 작성합니다.

> Why Google Style?
> 
> - **가독성**: 파라미터, 반환값, 예외 등이 명확히 구분되어 사람과 AI 모두 읽기 편합니다.
> - **표준화**: 널리 사용되는 표준으로, 린팅 도구 및 IDE(VS Code 등)의 호환성이 우수합니다.

**[작성 기준]**

- **Bad**: "이 함수는 가격을 리턴함" (단순 동작 설명)
- **Good**: "네이버 데이터 중 '패키지 요금'을 필터링하고 순수 '시간당 요금'만 추출하기 위함" (의도 및 이유 설명)

```python
def extract_room_pricing(item_details: dict) -> int:
    """합주실의 시간당 기본 요금을 추출합니다. (Summary)

    Args (인자 설명):
        item_details (dict): 네이버 예약 API의 개별 item 상세 정보.

    Returns (반환값 설명):
        int: 시간당 요금 (원 단위).

    Raises (예외):
        ValueError: 가격 정보 누락 시.

    Rationale (의도 - 필수):
        네이버 데이터 중 '패키지 요금' 필터링 및 순수 '시간당 요금'만 추출하기 위함.
        0원인 경우 신뢰도 문제로 로깅 후 예외 처리.
    """

```

### 1.2 커뮤니케이션 태그

코드 내 상태 표시를 위해 사용합니다.

- `TODO:`: 향후 구현/개선 필요
- `FIXME:`: 버그, 반드시 수정 필요
- `NOTE:`: 중요 참고 사항, 경고
- `HACK:`: 임시 코드 (리팩토링 대상)

---

## 2. Git & GitHub Workflow

**핵심 원칙: Issue -> Branch -> PR -> Review -> Merge**

### 2.1 이슈 (Issue)

- **제목 형식**: `<태그>: <설명>` (예: `feat: 사용자 프로필 API 구현`, `bug: can_reserve_one_hour 로직 오류`)
- **라벨**: 이슈 타입 라벨은 사용하지 않음. 운영 라벨(`review/24h`, `priority/high` 등)은 PR 단계에서 적용.
- **본문 구조** (`.github/ISSUE_TEMPLATE` 참조):

```markdown
### 📄 설명
(기능/버그에 대한 구체적 설명)

## 영향 범위
- (API, Repository, Service 등 영향받는 계층 나열)

## 특이사항
- (설계 결정 사항, 기획 협의 필요 사항 등)

### ✅ 작업할 내용
#### AS-IS
- (현재 문제점 서술)

#### TO-BE
- [ ] 체크박스 형태로 할 일 세분화
- [ ] ...

### 🙋🏻 참고 자료
- (링크, 스크린샷 등)
```

### 2.2 브랜치 (Branch)

- **형식**: `<태그>/<이슈번호>-<슬러그>`
- **예시**: `feat/123-login-api`, `fix/456-memory-leak`

**슬러그 작성 규칙:**

- **영문 소문자만** 사용 (`LoginUI` ❌ → `login-ui` ✅)
- **공백 대신 하이픈()** 사용
- **2~4 단어**, 핵심만 간결하게
- **도메인/기능 단위 먼저** (예: `auth-login-api`, `payment-webhook`)

### 2.3 커밋 메시지 (Commit Message)

- **형식**: `[이모지] [#이슈번호]; <태그>: <설명>`
- **예시**: `✨ [#123]; feat: 사용자 프로필 API 구현`
- **본문**: "무엇을", "왜" 변경했는지 상세 기술. 72자 내외 줄바꿈.

> Why Emojis?
텍스트만으로 이루어진 커밋 로그에서 변경 사항의 성격(기능 추가, 버그 수정, 문서 작업 등)을 시각적으로 빠르게 식별하기 위해 사용합니다. 또한 딱딱할 수 있는 개발 과정에 활력을 불어넣는 긍정적인 효과도 있습니다.
> 

> 이모지 사용 규칙:
> 
> - **Frontend**: 이모지 **필수** 사용
> - **Backend**: 이모지 **선택** (사용 권장)

**주요 태그 및 이모지 규칙:**

| 태그 | 이모지 | 설명 | 선택 기준 (Front vs Back) |
| --- | --- | --- | --- |
| `feat` | ✨ | 새로운 기능 | 공통: 새로운 기능 추가 |
| `fix` | 🔨 | 기능 수정 | Front: UI 수정으로 기능/버그 수정 시<br>Back: 기능 변경 (버그X) |
| `bug` | 🐛 | 버그 수정 | Front: (사용 X)<br>Back: 로직 오류/성능 이슈 해결 시 우선 사용 |
| `ui` | 💄 | UI 변경 | Front: 단순 디자인 변경 (기능 영향 X) |
| `refactor` | ♻️ | 리팩토링 | 공통: 기능 변화 없이 코드 구조 개선 |
| `docs` | 📝 | 문서 변경 | 공통: 주석, README 등 |
| `style` | 🎨 | 포맷팅 | 공통: 세미콜론, 줄바꿈 등 코드 영향 X |
| `test` | ✅ | 테스트 | 공통: 테스트 코드 추가/수정 |
| `chore` | 🔧 | 설정 변경 | 공통: 빌드, 패키지 매니저 설정 등 |

### 2.4 Pull Request (PR)

- **제목 형식**: `[#이슈번호]; <태그>: <설명>` (이모지는 **생략**)
- **예시**: `[#123]; feat: 사용자 로그인 기능 추가`
- **Review**: 최소 1명 이상의 승인(Approve) 필수.
    - Frontend → Frontend 팀 리뷰어
    - Backend → Backend 팀 리뷰어
- **Merge 전략**: `Rebase and merge` 사용 (선형 히스토리 유지).

---

## 3. 언어 및 스타일 (Language & Style)

### 3.1 한글 주석 원칙

- **주석/문서**: **한글** 작성을 원칙으로 하되, 기술 용어(API, JSON 등)는 **영어** 표기.
- **Why Korean?**
    - 팀원 간 **가장 빠르고 정확한 의사소통 수단**이 모국어이기 때문입니다.
    - 영어로 작성 시 발생할 수 있는 뉘앙스 차이나 오해를 줄이고, 복잡한 비즈니스 로직의 **의도(Reasoning)**를 명확히 전달하기 위함입니다.

### 3.2 어조 (Tone)

- `~함`, `~임` (명사형) 또는 `~합니다` (평서문) 중 선택하여 일관성 있게 작성합니다.